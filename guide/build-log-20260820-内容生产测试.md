# 构建日志 · 内容生产流程测试与修复

- 日期：2026-08-20
- 环境：本地 API `:18081`，`AI_MOCK_MODE=true`

## 修复

1. `domain_catalog` 改查 `config_json`；可选查询用 savepoint，避免误报 `geo_themes_not_migrated`
2. `start-produce` 按 `article_limit - created_count` 一次入队整包
3. Mock 产出 Wiki 合规正文（表格/内链/FAQ/速答），仿真无 KB 时用本稿作语料
4. 软/硬门禁：整包 `pack_gate_ok` 才标 published/measuring
5. Wiki MDX 不被渠道 `default_page_type=article` 改写 pack 页型

## 复测（Theme 19）

- from-scene → 确认 → 启生产：通过
- 4 篇 topic/concept/compare/guide，eval=passed，`pack_gate_ok=true`，status=measuring（有 remediation）
- Mock 正文 675 字，无提示词回声

单测：`tests/test_theme_pack_produce.py` + `test_geoweb_publisher.py`

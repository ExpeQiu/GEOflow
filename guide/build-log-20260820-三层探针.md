# 构建日志 · 三层探针 P0/P1/P2

- 日期：2026-08-20
- 方案：`技术品牌战术/GEO/系统设计/GEO三层探针体系方案.md`
- ADR：`guide/ADR/ADR-012-三层探针scheme与tracks.md`

## P0/P1

1. 冻结 `scheme` / `tracks` / `reasoning_grade` / `framework`（alembic 018）
2. 日扫锁定 C 轨 `open_api`；北极星 SQL 排除辅轨 metric
3. DeepSeek/豆包 `framework_api` 采集 `reasoning_content`；`AI_MOCK_MODE` 可跑
4. 主题挖掘禁止 snippet 冒充 A 轨；六类框架抽取 → 长尾 Query
5. Admin：框架轨扫描按钮；主题包展示六类框架

## P2

1. `citation_grounded`：`tracks=[B,C]`，`metric_kind=citation_sample`，北极星排除
2. 域名目录：`official_domains` + `GEOWEB_BASE_URL` + geoweb 渠道；source_hints 带 `owner`
3. Mock 引用轨答文含 GEOweb / 汽车之家 / 维基；`match_type` 可命中 official 或 wiki
4. 交叉判定：`mention_no_ours` / `framework_not_in_answer` / `ours_cited` / `gold`
5. Admin：引用轨扫描、探针设置域名、scheme 过滤、交叉表

## 验证

```
cd geoflow-v3/backend && .venv/bin/python -m pytest tests/test_probe_scheme.py tests/test_citation_scheme.py tests/test_cend_probe.py -q
```

未做：GLM 独立 A 轨连接器；豆包/Kimi 原生 web_search 工具（当前靠答文抽 URL + Mock）。

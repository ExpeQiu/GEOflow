# 构建日志 · 探针断路修复（日扫 / 官域 / 别名 / Theme）

- 日期：2026-08-25
- 对照：四套实扫断路 → 优化计划增量 1–3

## 做了什么

1. 情感 SQL 去掉 `json = json`；日扫后处理失败只打日志，探针 run 仍 completed。
2. `official_domains` 为空时保底 `geely.com,zeekrlife.com`。
3. `brand_aliases` 按 JSON 数组解析，避免「吉利银河」判成未提及。
4. 交叉 spawn Theme 写入 `meta.unused_dims` / `mining.brief`。

## 验证

```
cd geoflow-v3/backend && .venv/bin/python -m pytest tests/test_probe_ops_fixes.py tests/test_answer_parser.py tests/test_citation_scheme.py -q
```

Hub 日扫：`POST /api/admin/strategy/monitor/scan?scan_type=daily` → run **#62** completed，`post_error=None`，2 探针均 `mentioned=true`。Q52 交叉：`ours_cited`，`geely.com`/`zeekrlife.com` 归属 ours。Theme **#20** 已带 `unused_dims`。

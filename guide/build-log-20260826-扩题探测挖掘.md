# 构建日志 · 扩问题库 + 三轨探测 + 主题挖掘验证

- 日期：2026-08-26
- 对照：ADR-010 Theme 主链路；探针 Hub A/B/C

## 做了什么

1. 问题库由 9 题扩到 **29** 题（bulk 10 条高优 + 3 套模板展开 10 条）。
2. 日扫上限 `monitor_scan_limit=6`，平台 `deepseek`，`AI_MOCK_MODE=false`。
3. 实扫：C 日扫 run **#68**（6 探针）；A 框架 #69–71（三场景各 1，均为 `reasoning_grade=raw`）；B 引用 #72–74（`with_b=1`）。
4. 三场景 `compute-gap` → `from-scene` 草稿 Theme **#21/#22/#23**；交叉 spawn Theme **#24**（Q55，flags=`mention_no_ours`+`framework_not_in_answer`）。
5. 交叉 spawn 不再把探针原文写入 `target_queries`（原文留在 `meta.source_question_id`）。

## 验证

| 链路 | 结果 |
|------|------|
| from-scene 16/17/18 | draft，`llm_enhanced=true`，长尾 ≠ 探针全集 |
| thinking_digest | 三场景均有明文 A 轨摘要 |
| B 链 | 3 条 `citation_grounded` + 9 条 `url_dead` |
| 脚本 | `python3 geoflow-v3/scripts/expand_probe_mine.py` → OK |

日扫命中新题：Q55「收购沃尔沃口碑」、Q56「银河 E5 vs 宋 PLUS」。

## 续验（confirm → produce → 硬门禁）

脚本：`python3 geoflow-v3/scripts/continue_theme_chain.py`

| 步骤 | 结果 |
|------|------|
| 重启 API | spawn 修复生效 |
| confirm #21/#22/#23 | `hard`，Task 17/18/19，remediation 11/12/13；长尾未整集等于探针 |
| spawn Q55 → Theme #25 | 探针原文不进 `target_queries`；`unused_dims=['安全','耐久']` |
| start-produce #21 | topic/concept/compare/guide + article（#48/#51–54） |
| GeoEval | 5 篇 `advisory`；硬门禁 `pack_gate_ok=false`，停在 `producing` |

硬门禁不把 advisory 当过关，符合 ADR-010。继续分发需改 `gate_mode=soft` 或评分到 `passed`。## 续验 2（软门禁 → GEOweb 分发 → 再扫收口）

脚本：`python3 geoflow-v3/scripts/continue_theme_dist.py`

| 步骤 | 结果 |
|------|------|
| #21 `gate_mode=soft` + refresh-gate | `gate_passed`（5 篇 advisory 在 soft 下过关） |
| 发布 5 篇 | 全部 `published`，分发 job 成功 |
| GEOweb 公开页 | 5/5 HTTP 200（topic/concept/**compare**/guide/article） |
| `sync-pack` | 422：compare/guide 缺 `related≥3`、`faq≥3`（单篇 sync 已成功） |
| 再扫 remediation #11 | run **#75**；可见度 83.3→80.0（−3.3pp），Top3 70→100（+30pp） |
| Theme #21 | **`completed`** |

## 续验 3（整包 sync + #22/#23 生产收口）

脚本：`python3 geoflow-v3/scripts/continue_pack_22_23.py`

| 步骤 | 结果 |
|------|------|
| #21 compare/guide 补 `related≥3` + `faq≥3` | sync-pack 通过；MDX 已写入互链与 FAQ |
| #22 智驾 | 软门禁生产 4 篇，GEOweb 4/4 200；再扫 run #76；**completed** |
| #23 品牌 | 4 篇已发布；首次分发漏入队，补 `distribution/batch` 后 4 job 成功；再扫 run #77（HTTP 503 但库内已完成）；**completed** |

再扫 lift（可见度 / Top3）：

- #21 场景16：83.3→80.0 / 70→100
- #22 场景17：62.5→40.0 / 10→100（提及率下降，Top3 升高）
- #23 场景18：100→100 / —→80

三场景 Theme 均已 `completed`。#23 首次 `publish` 未落 `article_distributions`（Celery 忙于扫评），需补一次 batch。

## 注意

- 定时日扫现在每次 6 题（DeepSeek 费用高于原先 2）。

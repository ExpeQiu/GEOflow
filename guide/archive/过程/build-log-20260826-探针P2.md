# 构建日志 · 探针 P2（C 端口径 / B 链核验 / 调度时区）

- 日期：2026-08-26
- 对照：优化计划 P2

## 做了什么

1. C 端 skip（无 Playwright / 捕获失败）盖 `scheme=cend_sample`，tracks 为空，不再落成 `open_api`。
2. B 轨答文 URL 默认 HEAD/GET 存活核验；死链写入 `url_dead`，全部死亡则 evidence 降 L0。
3. 生产任务调度 `next_run_at` 改为 naive UTC，避免 asyncpg 时区混用。

## 验证

```
cd geoflow-v3/backend && .venv/bin/python -m pytest tests/test_probe_scheme.py tests/test_cend_probe.py tests/test_probe_ops_fixes.py -q
```

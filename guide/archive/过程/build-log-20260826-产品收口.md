# 构建日志 2026-08-26 · 产品收口

## 做了什么

- Wave A：三层 Hub 一级 Tab 收至 10 个；探针默认 open_api 日扫
- Wave B：`legacy_direct_task` → 410；任务默认 pipeline；渠道新建默认仅 GEOweb；删 SimulatorPanel
- Wave C：CLI 标归档；素材/文章加遗产提示
- Wave D：复测主闭环已通；补 Theme 关账 `RETURNING id` 日志与收口单测

## 日志键

`theme_id` / `pack_gate_ok` / `remediation_id` / `theme_ids` / `run_id`

## 验收

```bash
cd geoflow-v3 && ./scripts/verify-services.sh
cd backend && .venv/bin/python -m pytest tests/test_product_trim.py tests/test_theme_pack_produce.py::test_mining_prompt_injects_compare_dims -q
```

Admin 手点：诊断 → 探针日扫 → 主题包 → Wiki。旧 URL `/strategy/sales-copy`、`/operations/articles` 仍可用。

补丁：`zh.ts` 两处 `hubSubtitle` 缺逗号导致整站 `/login` 500，已修。

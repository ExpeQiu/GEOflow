# 构建日志 · 全链路卡点修补 P0 / P1

- 日期：2026-08-20
- 对照：知识库 GEO 主链路 + ADR-010/012

## 做了什么

1. `start-all` 拉起 Celery Worker + Beat；`verify.sh` / `verify-services.sh` 无 worker 记失败。
2. 主题挖掘 `meta.mining` 注入写作 prompt / workflow payload；Mock 对比表吃 `compare_dims`。
3. remediation 再扫完成后 Theme `measuring → completed`。
4. Theme/文章 slug 强制 ASCII；sync-pack 互链补 `related`、统一 `geo_theme_id`；hard 门禁拦截 sync-pack。
5. GEOweb sync / sync-pack 后 `revalidatePath` 机器出口；slug 拒中文。
6. Beat 周一 05:00 框架轨、05:30 引用轨。草稿按钮改为「确认主题」。

## 续：存量清洗 + P1（同日）

7. 已上线中文 slug 包改为 ASCII（`family-bev-decision*` / `content-produce-flow-test`），互链 related，article 的 `geo_theme_id` 收到 19。`ascii_slug`：有汉字且拉丁字母 <4 走 hash。
8. Theme lift 按 `probe_evidence` / `target_queries` 子集算 visibility，无题集才回退全场景。
9. 交叉判定信源/形态缺口可 `POST /strategy/cross-track/spawn-theme` 生成 Theme 草稿。
10. Techstore Technique 增加 `wikiUrl`；GEOweb 地图有 URL 则「查看 Wiki」。
11. 文档：解析文档 / 溯源链改写 GEOweb；PROBE-TRUTH 状态改为主栈已落地。
12. B 轨默认 L1 答文抽 URL；`CITATION_WEB_SEARCH=true` 时 Kimi 尝试 `$web_search`，失败回退。

## 验证

```
cd geoflow-v3/backend && .venv/bin/python -m pytest tests/test_wiki_editor.py tests/test_theme_pack_produce.py tests/test_geoweb_publisher.py tests/test_lift_scope.py tests/test_citation_scheme.py -q
```

# content-LangGraph-CLI 环境变量模板（勿提交真实密钥）

# Mock 模式（与 --demo / --mock 一致语义）
CONTENT_LG_MOCK_MODE=true

# 可选：覆盖默认 config/ 目录
# CONTENT_LG_CONFIG_DIR=/absolute/path/to/config

# 后续接真实 LLM 时使用（第一期未启用）
# CONTENT_LG_OPENAI_API_KEY=
# CONTENT_LG_OPENAI_BASE_URL=
# CONTENT_LG_MODEL=

# GEOFlow v3 后续待办清单

> 更新：2026-08-13  
> 基准：Wave A–D（AIVIS）已完成；**P0 真实闭环**已落地；**总览北极星 KPI（Wave A–E 主路径）**已落地；Wave 9–12 增强项仍部分待办。

---

## 已完成里程碑（摘要）

| Wave | 内容 | 状态 |
|------|------|------|
| Wave 0–7 | 基础闭环、Monitor 探针 v1 | ✅ |
| C 端辅轨 | Playwright 六平台 + api L1 引用落库 + Admin 触发 | ✅ 2026-08 |
| **Wave A–D** | AIVIS 场景/缺口/报告 | ✅ |
| **P0 真实闭环** | strict API、缺口 RAG 阈值、remediation lift、Gweb 对齐 | ✅ |
| **总览对齐 KPI** | Top3/pp、质量灯、信源章、ΔTop3、销售口径 API、014 | ✅ 2026-08-13 |

**部署提醒**：`alembic upgrade head`（至 **`015_probe_gold_labels`**）；生产 `AI_MOCK_MODE=false` + `monitor_probe_mode=api` + `monitor_strict_api=true`。

---

## 总览对齐（已完成）

| ID | 任务 | 关键文件 |
|----|------|----------|
| KPI-A | Top3/pp 聚合 + ProbeDashboard + 报告北极星章 | `north_star_kpi.py`、`monitor_probe.py`、`ProbeDashboard.tsx`、`report_composer.py` |
| KPI-B | 参数一致率硬门槛 + quality 章 + open_api 脚注 | `quality_metrics_service.py` |
| KPI-C | remediation ΔTop3 + 信源占比 | `remediation_service.py`、`source_metrics_service.py` |
| KPI-D | Embedding 生产就绪检查 + 切流 Runbook | `embeddings.py`、`guide/cutover-runbook.md` |
| KPI-E | 销售口径 SSOT API + GeoEval 明细 + apply 写回 | `sales_copy_service.py`、admin routes |

---

## 专题 PROBE-TRUTH（立项）

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 报告口径 `open_api` 分离脚注 | ✅ |
| M1 | daily_limit + priority 批次 | ✅ 2026-08-13 |
| M2 | Rank v2 list_order | 部分（夹具 ≥80%） |
| M3 | Citation L1 evidence_level | 部分 |
| M4 | 金标对照表 | ✅ 表 015 + Admin/API/报告脚注 |

### M1/M4 落地摘要

- **priority 批次**：daily 默认 `priority >= probe_priority_floor_daily`（80）；market ≥50
- **daily_limit**：探针 api/llm 前检查 `ai_models.daily_limit`，超限 `engine=skipped`，禁止 corpus 填洞；成功后 bump `used_today`
- **金标**：`geo_probe_gold_labels`；`GET/POST /strategy/gold-labels`、`/bias`、JSONL 导入；报告 closed_loop 注入偏移脚注
- **非 mock**：数据采集页清单；生产 `AI_MOCK_MODE=false` + `probe_mode=api` + `strict_api` + API Key

| PROBE-TRUTH M1/M4 · Admin 接线 | ✅ |
| 全平台 Connector 真 Key E2E（通义/文心/Kimi/元宝） | 待 UAT |

---

## Wave 9 / 10 / 11 剩余

- W9-2/3/6 上传分块、sandbox、url_import 真图：待办
- W9-4/5 PDF OCR / DOCX：已有
- W10 Publisher 端到端与切流执行：Runbook 已写
- W11 GeoEval drill-down API / apply 写回 / 销售口径：已完成主路径

### 策略 Admin 接线（2026-08-13）

| 项 | 状态 |
|----|------|
| apply-recommendations 前端 stub | ✅ 已接线 |
| WebIntel / settings 死链 | ✅ 已挂载 / 重定向到 collection |
| StrategyOverview（remediation / GEOweb） | ✅ 挂入诊断总览 |
| sales-copy UI | ✅ Hub Tab |
| scene compute-gap / TJG 导入 / 市场量配置 | ✅ |
| 错误信息细化 | ✅ api-client |
| PROBE-TRUTH M1/M4 · 非 mock 护栏 | ✅ |
| 通义等 6 平台真 Key 全量 E2E | 待 UAT |
---

## 快速入口

| 资源 | 路径 |
|------|------|
| 验证报告 | [`guide/verification-report.md`](./verification-report.md) |
| 切流 Runbook | [`../../guide/cutover-runbook.md`](../../guide/cutover-runbook.md) |
| 启动/验证 | `scripts/start.sh`、`scripts/verify.sh` |

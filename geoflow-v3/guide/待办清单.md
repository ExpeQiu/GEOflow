# GEOFlow v3 后续待办清单

> 更新：2026-07-14  
> 基准：Wave A–D（AIVIS）已完成；**P0 真实闭环**已落地（见 `guide/真实闭环P0.md`）；Wave 9–12 仍待实施。

---

## 已完成里程碑（摘要）

| Wave | 内容 | 状态 |
|------|------|------|
| Wave 0–7 | 基础闭环、Monitor 探针 v1 | ✅ |
| **Wave A–D** | AIVIS 场景/缺口/报告 | ✅ |
| **P0 真实闭环** | strict API、缺口 RAG 阈值、remediation lift、Gweb 对齐 | ✅ 2026-07-14 |

**部署提醒**：`alembic upgrade head`（至 **`012_closed_loop_remediation`**）；生产 `AI_MOCK_MODE=false` + `monitor_probe_mode=api` + `monitor_strict_api=true`。

---

## P0 真实闭环（已完成）

| ID | 任务 | 关键文件 |
|----|------|----------|
| P0-1 | strict_api + engine_mix + corpus citation 隔离 | `registry.py`、`monitor_probe.py` |
| P0-2 | remediation 表 + 发布钩子 + delayed rescan + 报告章 | `012_*`、`remediation_service.py`、`report_composer.py` |
| P0-3 | 缺口双通道 score 阈值 | `scene_gap_analyzer.py` |
| P0-4 | Gweb pages.json 对齐率 | `gweb_alignment_service.py` |

---

## 专题 PROBE-TRUTH（立项）

> 详见 [`guide/探针真值标定专题.md`](../../guide/探针真值标定专题.md)

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 报告口径 `open_api` / `cend_sample` 分离 | 待办 |
| M1 | daily_limit + priority 批次 + skipped 可观测（W8-6+） | 待办 |
| M2 | Rank v2 list_order + `rank_method` | 待办 |
| M3 | Citation L1（答文 URL）+ evidence_level | 待办 |
| M4 | 金标对照表 + 月度偏移报告 | 待办 |

---

## Wave A–D（AIVIS 升级）— 已完成

| ID | 任务 | 关键文件 |
|----|------|----------|
| A1 | 007 migration | `alembic/versions/007_aivis_foundation.py` |
| A2 | PlatformConnector + 加权排名 | `platform_connectors/`、`monitor_probe.py` |
| A3 | 模板/场景/竞品 API + MonitorPanel Tab | `monitor_aivis_service.py`、`MonitorPanel.tsx` |
| B1 | scene_gap_analyzer | `scene_gap_analyzer.py` |
| B2 | gap_task_generator | `gap_task_generator.py` |
| B3 | 豆包/DeepSeek API | `api_connector.py` |
| C1 | SentimentAnalyzer | `sentiment_analyzer.py` |
| C2 | CompetitiveAnalyzer + market-scan | `competitive_analyzer.py`、`monitor_scan.py` |
| C3 | InsightGenerator + 告警 | `insight_generator.py`、`monitor_alerts.py` |
| D1 | ReportComposer | `report_composer.py` |
| D2 | 报告页 + 趋势 + Overview | `strategy/monitor/reports/page.tsx` |

---

## Wave 8 — Monitor 与策略深化（P1）— 已并入 A–D

原 W8-1~W8-6 能力已在 Wave A–D 实现；剩余增强项：

| ID | 任务 | 状态 |
|----|------|------|
| W8-6+ | LLM 探针按 ai_models.daily_limit 限流 | 待办 |
| W8+ | 通义/文心/Kimi/元宝 API Connector | 待办（骨架已预留） |

---

## 原 Wave 8 条目（归档参考）

~~W8-1 真实平台 API~~ → Wave B（豆包/DeepSeek）  
~~W8-2 趋势图表~~ → Wave D（snapshots Tab）  
~~W8-3 定时扫描 UI~~ → Wave A（monitor_scan_limit settings）  
~~W8-4 告警联动~~ → Wave C  
~~W8-5 market-scan~~ → Wave C  
~~W8-6 LLM 配额~~ → 部分（降级链已实现）

---

## Wave 9 — RAG 与知识生产（P1）

| ID | 任务 | 说明 | 验收标准 |
|----|------|------|----------|
| W9-1 | 关闭 Mock 的生产 Embedding | `AI_MOCK_MODE=false` 时向量化链路全通 | 上传知识库后 chunk 可查、RAG 召回非 mock |
| W9-2 | 上传自动分块同步 | 知识库文件/PDF 上传后触发 `KnowledgeChunkSync` | 上传完成 ≤1min 内 sandbox 可检索新内容 |
| W9-3 | RAG sandbox 增强 | 展示召回片段、相似度、来源文章/知识库 | 专页显示 top-k chunks 与 score |
| W9-4 | 扫描版 PDF OCR | 无文本层 PDF 可选 Tesseract / 云 OCR 降级 | 扫描 PDF 可提取正文或明确错误提示 |
| W9-5 | DOCX / HTML 知识导入 | 扩展 `upload_service` 支持常见办公格式 | .docx 上传可追加到知识库 content |
| W9-6 | url_import 生产 workflow | 非 mock 时走真实 LangGraph url_import 图 | 结构化字段与 mock 契约一致 |

**关键文件**：`embeddings.py`、`rag/`、`upload_service.py`、`workflow_runner.py`

---

## Wave 10 — 切流与外部集成（P0）

| ID | 任务 | 说明 | 验收标准 |
|----|------|------|----------|
| W10-1 | 分发 Publisher 完整移植 | WordPress / Generic HTTP / Agent 渠道真实发布 | 三渠道各 1 篇端到端发布成功 |
| W10-2 | v1 materials 六类型 API | 标题库/关键词库/图片库等外部契约 1:1 兼容 Laravel | 契约测试或 Postman 集合全绿 |
| W10-3 | C3 Laravel 002 数据迁移 | 扩展 `migrate` 脚本覆盖历史业务数据 | 迁移报告 + 抽样校验 |
| W10-4 | 反代切换与 Laravel 归档 | Nginx/网关切到 v3，旧栈只读归档 | 切流 Runbook + 回滚步骤 |
| W10-5 | 幂等键全面覆盖 | 写操作 API 统一 `Idempotency-Key` 头 | 重复请求不产生重复记录 |
| W10-6 | content_agent 回调生产化 | 外部 Agent 回调验签、超时、重试 | 回调落库 + 任务状态闭环 |

**关键文件**：`distribution/`、`api/v1/`、`scripts/migrate_*`、`content_agent_request_service.py`

---

## Wave 11 — GeoEval 与运营体验（P2）

| ID | 任务 | 说明 | 验收标准 |
|----|------|------|----------|
| W11-1 | GeoEval 诊断专页 | 失败原因 drill-down、单篇评估明细 | `/strategy/geo-eval` 可点进文章评估详情 |
| W11-2 | 自动发布门禁（软默认） | 默认 soft：仿真只给评分/建议；`GEO_EVAL_HARD_GATE=true` 才拦自动分发 | 软模式 advisory 可发布；硬模式 failed 拦截可审计 |
| W11-3 | Simulator 应用建议落地 | `apply_recommendations` 写回文章 metadata / 标题 | 一键应用后文章字段可见变更 |
| W11-4 | 采纳看板趋势 | `aggregate_adoption_metrics` 前端图表化 | Analytics 页展示采纳率趋势 |
| W11-5 | Insight 自动 re-mine 调度 | 按模板 source_url 变更触发 Worker | 定时或手动触发均有状态回写 |
| W11-6 | WebIntel 报告历史 | `web_insight_reports` 列表与详情页 | 策略 Hub 可浏览历史抓取报告 |

---

## Wave 12 — 前端与工程质量（P2–P3）

| ID | 任务 | 说明 | 验收标准 |
|----|------|------|----------|
| W12-1 | Admin 六语言 i18n | 补齐 zh 外 en/ja 等（verification-report UAT） | 语言切换无硬编码中文漏网 |
| W12-2 | Shadcn UI 全面替换 | Hub 页统一组件库与暗色模式 | 主要 Hub 视觉一致 |
| W12-3 | pytest 套件修复 | 补齐 `bcrypt` 等依赖，核心 service 单测 | CI `pytest` 绿 |
| W12-4 | verify-admin 扩展 | 独立 Admin smoke 脚本覆盖写操作 | 一键验证 CRUD + Worker 入队 |
| W12-5 | 任务 WebSocket 稳定性 | 断线重连、心跳、多 Tab 去重 | 长时间开着任务页状态仍准确 |
| W12-6 | 更新 verification-report | 同步 Wave 5–7 完成项与 UAT 清单 | 文档与代码状态一致 |

---

## 技术债与已知限制

- [x] ~~Monitor 探针无真实 API / 静默 corpus 污染~~ → P0：`strict_api` + engine_mix（Chat API 仍≠ C 端联网回答）
- [x] ~~缺口 Task 无 lift 复测~~ → P0：`geo_gap_remediation_runs`
- [x] ~~Gweb pages.json 未回流~~ → P0：`/strategy/monitor/gweb-alignment`
- [ ] 通义/文心/Kimi/元宝真实 API Connector 仍待办
- [ ] `AI_MOCK_MODE=true` 为默认，生产需显式关闭并配置真实模型 Key
- [ ] LLM 探针模式仅处理前 5 个高优先级问题（防 rate limit）；strict_api 下会 skipped
- [ ] 图片/知识库上传无病毒扫描与 MIME 深度校验
- [ ] 部分 Alembic 迁移需手动 `upgrade head`（现至 **012**）

---

## 建议排期（人天估算）

| Wave | 聚焦 | 预估 |
|------|------|------|
| Wave 8 | Monitor 真实化 + 趋势 | 4–6d |
| Wave 9 | RAG 生产化 | 4–5d |
| Wave 10 | 切流底座 | 10–15d |
| Wave 11 | GeoEval / 运营 | 5–7d |
| Wave 12 | 前端 / 工程质量 | 5–8d |

**至生产切流（Wave 8–10）**：约 **18–26 人天**  
**至体验完整（含 Wave 11–12）**：约 **28–41 人天**

---

## 每 Wave 统一 DoD

1. 相关 API 在 `scripts/verify.sh` 或 `verify-admin.sh` 中有 smoke 用例
2. 关键路径打结构化日志（入队、CRUD、Worker 结果）
3. 更新 [`verification-report.md`](./verification-report.md) 完成项
4. 不新增无引用的 Placeholder 文案
5. 需要 migration 时在 PR 说明中注明 revision 号

---

## 快速入口

| 资源 | 路径 |
|------|------|
| 架构说明 | [`guide/architecture.md`](./architecture.md) |
| 验证报告 | [`guide/verification-report.md`](./verification-report.md) |
| Admin 路由 | `backend/app/api/admin/routes.py` |
| Worker 任务 | `backend/app/workers/tasks.py` |
| 启动/验证 | `scripts/start.sh`、`scripts/verify.sh` |
